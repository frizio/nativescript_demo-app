<Page class="page" loaded="onLoaded">

    <Page.actionBar>
        <ActionBar title="The Demo App" icon="" class="action-bar">
        </ActionBar>
    </Page.actionBar>

    <FlexboxLayout class="p-10" backgroundColor="#eee" flexDirection="column" justifyContent="center" alignItems="stretch" flexWrap="wrap" >
        <TextField marginBottom="10" hint="Enter the username" text="{{user.username}}" />
        <TextField marginBottom="10" hint="Enter the password" secret="true" text="{{user.password}}" />
        <Label marginBottom="10" text="{{user.username}}" textWrap="true" />
        <Button text="tap me" tap="onTap" />
    </FlexboxLayout>

</Page>

